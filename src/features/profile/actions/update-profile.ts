import {
  ActionState,
  toErrorActionState,
} from '@/components/form/utils/to-action-state';

export function updateProfile(actionState: ActionState, formData: FormData) {
  // TODO: Implementation for updating the user profile
  return toErrorActionState('not implemented');
}
